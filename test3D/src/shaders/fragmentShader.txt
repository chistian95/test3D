#version 400 core

in vec3 color;
in vec2 pasar_coordsTextura;

out vec4 color_fuera;

uniform sampler2D sampleadorTextura;

void main(void) {
	color_fuera = texture(sampleadorTextura, pasar_coordsTextura);
}